apiVersion: v1
kind: Service
metadata:
  labels:
    app: collaboration-service
  name: collaboration-service
spec:
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: collaboration-service